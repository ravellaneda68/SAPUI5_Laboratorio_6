_schema-version: '3.2'
ID: invoices6
version: 0.0.1
modules:
  - name: invoices6-approuter
    type: approuter.nodejs
    path: invoices6-approuter
    requires:
      - name: invoices6_html_repo_runtime
      - name: dest_invoices6
    parameters:
      disk-quota: 256M
      memory: 256M
  - name: invoices6_ui_deployer
    type: com.sap.application.content
    path: .
    requires:
      - name: invoices6_html_repo_host
        parameters:
          content-target: true
    build-parameters:
      build-result: resources
      requires:
        - artifacts:
            - invoices6-content.zip
          name: invoices6
          target-path: resources/
  - name: invoices6
    type: html5
    path: invoices6
    build-parameters:
      builder: custom
      commands:
        - npm run build
      supported-platforms: []
resources:
  - name: invoices6_html_repo_runtime
    type: org.cloudfoundry.managed-service
    parameters:
      service: html5-apps-repo
      service-plan: app-runtime
  - name: invoices6_html_repo_host
    type: org.cloudfoundry.managed-service
    parameters:
      service: html5-apps-repo
      service-plan: app-host
  - name: dest_invoices6
    parameters:
      service-plan: lite
      service: destination
    type: org.cloudfoundry.managed-service
build-parameters:
  before-all:
    - builder: custom
      commands:
        - npm install
